# bosd

The BikeOS daemon.

## Build

```sh
go get github.com/bikeos/bosd/cmd/bosd
```

## Run

### GPS

Read the GPS time:

```sh
bosd gps date --set
```

Set the system time to GPS time:

```sh
bosd gps date --set
```